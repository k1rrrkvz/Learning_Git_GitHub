# 🚀 Краткий мануал: Git от локального проекта до GitHub

## 📋 Предварительные шаги (один раз)

### 1. Настройка Git (если ещё не делали)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваша@почта.com"
git config --global core.autocrlf true  # Для Windows
```

### 2. Создание Personal Access Token на GitHub

1. Зайдите на [github.com/settings/tokens](https://github.com/settings/tokens)
2. **Generate new token** → **Generate new token (classic)**
3. Выберите **repo** (полный контроль репозиториев)
4. Скопируйте токен — он покажется только один раз!

---

## 🔄 Основной рабочий процесс

### ШАГ 1: Создать репозиторий на GitHub

1. Нажмите **+** → **New repository**
2. Укажите имя (например, `my-project`)
3. **НЕ ставить галочку** "Add a README.md"
4. Нажмите **Create repository**

### ШАГ 2: Локальная инициализация проекта

```bash
# 1. Перейти в папку проекта
cd путь/к/проекту

# 2. Инициализировать Git репозиторий
git init

# 3. Добавить все файлы
git add .

# 4. Создать первый коммит
git commit -m "Initial commit"
```

### ШАГ 3: Связать с GitHub

```bash
# 1. Добавить удалённый репозиторий
git remote add origin https://github.com/ВАШ_ЛОГИН/НАЗВАНИЕ_РЕПО.git

# 2. Переименовать основную ветку (если нужно)
git branch -M main

# 3. Загрузить код на GitHub
git push origin main
```

### ШАГ 4: При первом push

1. Введите ваш GitHub username
2. В качестве пароля — вставьте **Personal Access Token**
3. Нажмите Enter

---

## 📝 Ежедневные команды

### Добавление новых изменений

```bash
# 1. Проверить статус
git status

# 2. Добавить изменения
git add имя_файла     # конкретный файл
git add .             # все изменения

# 3. Закоммитить
git commit -m "Описание изменений"

# 4. Загрузить на GitHub
git push origin main
```

### Получение обновлений с GitHub

```bash
# Скачать изменения
git pull origin main
```

---

## 🆘 Частые проблемы и решения

### Ошибка: "Updates were rejected"

```bash
# Сначала получить изменения
git pull origin main

# Если есть конфликты, исправить их, затем:
git add .
git commit -m "Resolve conflicts"
git push origin main
```

### Ошибка: "src refspec main does not match any"

```bash
# Создать коммит перед push
git add .
git commit -m "First commit"
git push origin main
```

### Забыли добавить удалённый репозиторий

```bash
# Проверить подключённые репозитории
git remote -v

# Добавить если нет
git remote add origin URL_РЕПОЗИТОРИЯ
```

---

## 📁 Полезные команды

```bash
# Просмотр истории коммитов
git log --oneline

# Просмотр изменений в файлах
git diff

# Просмотр всех веток
git branch -a

# Создание новой ветки
git checkout -b новая-ветка

# Клонирование чужого репозитория
git clone URL_РЕПОЗИТОРИЯ
```

---

## ✅ Чек-лист успешной публикации

- [ ] Репозиторий создан на GitHub (без README)
- [ ] `git init` выполнен в папке проекта
- [ ] Файлы добавлены (`git add .`)
- [ ] Создан коммит (`git commit -m "..."`)
- [ ] Добавлен origin (`git remote add origin ...`)
- [ ] Ветка переименована в main (если нужно)
- [ ] `git push origin main` выполнен успешно
- [ ] Personal Access Token использован как пароль

---

## 🎯 Быстрая команда для новых проектов

```bash
cd проект
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ЛОГИН/РЕПОЗИТОРИЙ.git
git branch -M main
git push origin main
```

**Важно:** При первом push использовать Personal Access Token вместо пароля!

---

## 💡 Советы

- ✓ Всегда пишите понятные сообщения коммитов
- ✓ Делайте `git status` перед важными операциями
- ✓ Регулярно делайте `git push` чтобы не потерять код
- ✓ Используйте `.gitignore` для исключения ненужных файлов

---

**Теперь ваш код всегда будет сохранён и на компьютере, и на GitHub!** 🎉

---

## 📚 Дополнительные материалы

Для более подробного изучения смотрите:
- **git_guide.md** — полная инструкция по Git и GitHub
- **practice_01-10** — практические задания
- **practice_11_github_workflow.md** — работа с GitHub

**Это краткая шпаргалка:** practice_12_quick_reference.md
